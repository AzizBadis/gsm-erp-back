import{Injectable}from'@nestjs/common';import{AccountTransactionDirection}from'@prisma/client';import{PaginationDto}from'../../common/dto/pagination.dto';import{PrismaService}from'../../prisma/prisma.service';import{CreateAccountTransactionDto,CreatePaymentAccountDto}from'./dto/payment-account.dto';@Injectable()export class PaymentAccountsService{constructor(private readonly prisma:PrismaService){}async accounts(){const rows=await this.prisma.paymentAccount.findMany({include:{transactions:true},orderBy:{name:'asc'}});return rows.map(a=>({...a,balance:Number(a.initialBalance)+a.transactions.reduce((s,t)=>s+(t.direction===AccountTransactionDirection.CREDIT?Number(t.amount):-Number(t.amount)),0)}))}createAccount(dto:CreatePaymentAccountDto,createdBy:string){return this.prisma.paymentAccount.create({data:{...dto,createdBy}})}async transactions(q:PaginationDto){const where=q.search?{OR:[{reference:{contains:q.search,mode:'insensitive' as const}},{invoiceReference:{contains:q.search,mode:'insensitive' as const}},{description:{contains:q.search,mode:'insensitive' as const}},{account:{name:{contains:q.search,mode:'insensitive' as const}}}]}:{};const[data,total]=await Promise.all([this.prisma.accountTransaction.findMany({where,include:{account:true},skip:(q.page-1)*q.limit,take:q.limit,orderBy:{transactionDate:'desc'}}),this.prisma.accountTransaction.count({where})]);return{data,total,page:q.page,limit:q.limit}}createTransaction(dto:CreateAccountTransactionDto,createdBy:string){return this.prisma.accountTransaction.create({data:{...dto,transactionDate:dto.transactionDate?new Date(dto.transactionDate):new Date(),createdBy},include:{account:true}})}async trialBalance(){const accounts=await this.prisma.paymentAccount.findMany({include:{transactions:true},orderBy:{name:'asc'}});return accounts.map(a=>({id:a.id,name:a.name,debit:a.transactions.filter(t=>t.direction===AccountTransactionDirection.DEBIT).reduce((s,t)=>s+Number(t.amount),0),credit:Number(a.initialBalance)+a.transactions.filter(t=>t.direction===AccountTransactionDirection.CREDIT).reduce((s,t)=>s+Number(t.amount),0)}))}}
