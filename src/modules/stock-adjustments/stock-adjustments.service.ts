import{BadRequestException,Injectable}from'@nestjs/common';import{StockAdjustmentType,StockMovementType}from'@prisma/client';import{PaginationDto}from'../../common/dto/pagination.dto';import{PrismaService}from'../../prisma/prisma.service';import{CreateStockAdjustmentDto}from'./dto/stock-adjustment.dto';
@Injectable()export class StockAdjustmentsService{constructor(private readonly prisma:PrismaService){}async findAll(q:PaginationDto){const where=q.search?{OR:[{reference:{contains:q.search,mode:'insensitive' as const}},{location:{contains:q.search,mode:'insensitive' as const}},{reason:{contains:q.search,mode:'insensitive' as const}}]}:{};const[data,total]=await Promise.all([this.prisma.stockAdjustment.findMany({where,include:{items:{include:{product:true}}},skip:(q.page-1)*q.limit,take:q.limit,orderBy:{adjustmentDate:'desc'}}),this.prisma.stockAdjustment.count({where})]);return{data,total,page:q.page,limit:q.limit}}async create(dto:CreateStockAdjustmentDto,addedBy:string){return this.prisma.$transaction(async tx=>{const products=await tx.product.findMany({where:{id:{in:dto.items.map(i=>i.productId)}}});if(products.length!==new Set(dto.items.map(i=>i.productId)).size)throw new BadRequestException('One or more products were not found');if(dto.type===StockAdjustmentType.SUBTRACT){for(const item of dto.items){const p=products.find(x=>x.id===item.productId)!;if(p.stockQty<item.quantity)throw new BadRequestException(`Insufficient stock for ${p.name}`)}}const count=await tx.stockAdjustment.count();const reference=dto.reference?.trim()||`SA-${new Date().getFullYear()}-${String(count+1).padStart(5,'0')}`;const total=dto.items.reduce((s,i)=>s+i.quantity*Number(products.find(p=>p.id===i.productId)!.unitPrice),0);const adjustment=await tx.stockAdjustment.create({data:{reference,adjustmentDate:dto.adjustmentDate?new Date(dto.adjustmentDate):new Date(),location:dto.location,type:dto.type,total,recoveredAmount:dto.recoveredAmount??0,reason:dto.reason,addedBy,items:{create:dto.items.map(i=>{const p=products.find(x=>x.id===i.productId)!;return{productId:i.productId,quantity:i.quantity,unitPrice:p.unitPrice,lineTotal:i.quantity*Number(p.unitPrice)}})}},include:{items:{include:{product:true}}}});for(const item of dto.items){const delta=dto.type===StockAdjustmentType.ADD?item.quantity:-item.quantity;await tx.product.update({where:{id:item.productId},data:{stockQty:{increment:delta}}});await tx.stockMovement.create({data:{productId:item.productId,type:StockMovementType.ADJUSTMENT,quantity:delta,reason:`${reference}: ${dto.reason??dto.type}`}})}return adjustment})}}
